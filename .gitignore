# Dependency directories
node_modules/

# Build outputs
dist/
build/
coverage/
.nyc_output/

# Cache / tool artifacts
.cache/
.parcel-cache/
.turbo/
.eslintcache
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files (keep only examples)
.env
.env.local
.env.*.local
!.env.example

# OS / Editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json

# Prisma (keep schema & migrations, ignore generated outputs if any)
.prisma/
src/generated/prisma/

# Temporary / misc
tmp/
temp/

# Framework / platform specific (add if adopted later)
.next/
.vercel/
firebase-debug.log

# Lock files (keep the one you use; comment out others if switching)
yarn.lock
package-lock.json
pnpm-lock.yaml

# Optional: ignore test snapshots if not needed
# *.snap

